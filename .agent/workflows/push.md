---
description: Push changes to GitHub after completing a task
---

After completing any code changes, always push to GitHub:

// turbo
1. Stage all changes: `git add .`

// turbo
2. Commit with a descriptive message: `git commit -m "<descriptive message>"`

// turbo
3. Push to remote: `git push`
